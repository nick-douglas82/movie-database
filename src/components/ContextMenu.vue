<script setup lang="ts">
import { Menu, MenuButton, MenuItems, MenuItem } from '@headlessui/vue'
import { DotsVerticalIcon, TrashIcon, PencilIcon } from '@heroicons/vue/solid'
</script>

<template>
  <div class="absolute bottom-10 right-2">
    <Menu as="div" class="relative inline-block text-left">
      <div>
        <MenuButton>
          <div
            class="p-1 transition duration-300 ease-in-out bg-white rounded-full cursor-pointer w-7 h-7 hover:bg-green-300"
          >
            <DotsVerticalIcon />
          </div>
        </MenuButton>
      </div>

      <transition
        enter-active-class="transition duration-100 ease-out"
        enter-from-class="transform scale-95 opacity-0"
        enter-to-class="transform scale-100 opacity-100"
        leave-active-class="transition duration-75 ease-in"
        leave-from-class="transform scale-100 opacity-100"
        leave-to-class="transform scale-95 opacity-0"
      >
        <MenuItems
          class="absolute right-0 w-56 mt-2 origin-top-right bg-white divide-y divide-gray-100 rounded-md shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none"
        >
          <div class="px-1 py-1">
            <MenuItem v-slot="{ active }">
              <button
                @click="$emit('editName')"
                :class="[
                  active ? 'bg-green-300' : 'text-gray-900',
                  'group flex rounded-md items-center w-full px-2 py-2 text-sm',
                ]"
              >
                <PencilIcon :active="active" class="w-5 h-5 mr-2 text-pink-600" aria-hidden="true" />
                Edit Name
              </button>
            </MenuItem>
          </div>
          <div class="px-1 py-1">
            <MenuItem v-slot="{ active }">
              <button
                @click="$emit('deleteList')"
                :class="[
                  active ? 'bg-green-300' : 'text-gray-900',
                  'group flex rounded-md items-center w-full px-2 py-2 text-sm',
                ]"
              >
                <TrashIcon :active="active" class="w-5 h-5 mr-2 text-pink-600" aria-hidden="true" />
                Delete
              </button>
            </MenuItem>
          </div>
        </MenuItems>
      </transition>
    </Menu>
  </div>
</template>
